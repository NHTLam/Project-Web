{% extends "base.html.twig" %}
{% block body %}
    <div>
        <table>
            <thead>
                <tr>
                    <th>Title</th>
                    <th>File</th>
                    <th>Deadline</th>
                    <th>Student</th>   
                    <th>Menu</th>                 
                </tr>
            </thead>
            <tbody>
                {% for assignments in assignment %}
                    <tr>
                        <td>
                            <a href="{{ path('view_assignment_by_id', {'id': assignment.id}) }}">
                                {{ assignment.title }}
                            </a>
                        </td>
                        <td>{{ assignment.file }}</td>
                        <td>{{ assignment.dateSubmit | date('d/m/Y') }}</td>
                        <td>{{ assignment.student }}</td>
                        <td>         
                            <a href="{{ path('edit_assignment', {'id': assignment.id})}}">Edit</a>               
                            <a href="{{ path('delete_assignment', {'id': assigmemt.id})}}"
                            onclick = "return confirm('Do you want to delete this assigmemt');"
                            >Delete</a>                        
                        </td>
                    </tr>
                {% endfor %}
            </tbody>            
        </table>
    </div>
{% endblock %}