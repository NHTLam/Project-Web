{% form_theme classForm 'bootstrap_5_layout.html.twig' %}
{% extends 'base.html.twig' %}

{% block title %}{{title}} page{% endblock %}

{% block body %}
<div class = "container-fluid row">
    <div class = "col-lg-2">
        <ul class = "navbar-nav">
            <a href="{{path('view_course')}}">Courses management</a>
            <a href="{{path('view_class')}}">Classes management</a>
        </ul>
    </div>
    <div class = "col-lg-10">
        <h1 class = "text text-primary">{{title}} Class</h1>
        {{form(classForm)}}
    </div>
</div>
{% endblock %}
